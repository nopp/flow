apps:
    - id: app-teste
      name: app-teste
      repo: https://github.com/nopp/app-teste.git
      branch: main
      steps:
        - name: test
          cmd: go test ./...
          sleep_sec: 0
        - name: build
          cmd: go build -o .
          sleep_sec: 10
        - name: uname
          cmd: uname -a
          sleep_sec: 0
