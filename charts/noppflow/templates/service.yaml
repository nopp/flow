apiVersion: v1
kind: Service
metadata:
  name: {{ include "noppflow.fullname" . }}
  labels:
    {{- include "noppflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "noppflow.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
